---
import PixelLabel from '../ui/PixelLabel.astro';
import PixelButton from '../ui/PixelButton.astro';
import PostCard from '../PostCard.astro';
import { getPublishedPosts, postUrl } from '../../lib/posts';

const posts = (await getPublishedPosts()).slice(0, 3);
---

<section class="container-page py-16 lg:py-24" aria-labelledby="blog-title">
  <div class="mb-8 flex flex-wrap items-end justify-between gap-4">
    <div>
      <PixelLabel as="h2"><span id="blog-title">Book and Quill</span></PixelLabel>
      <p class="mt-4 max-w-xl text-ink-soft">I write down what I learn along the way.</p>
    </div>
    <PixelButton href="/blog" tone="paper">All posts</PixelButton>
  </div>

  <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
    {posts.map((post) => (
      <PostCard
        url={postUrl(post)}
        title={post.data.title}
        description={post.data.description}
        pubDate={post.data.pubDate}
        tags={post.data.tags}
        readingMinutes={post.data.ttr}
      />
    ))}
  </div>
</section>
