---
/**
 * Wraps markdown tables in a scrollable region. This keeps table semantics
 * intact while wide tables scroll instead of wrapping.
 */
const props = Astro.props;
---

<div class="table-scroll" tabindex="0" role="region" aria-label="Table, scrolls horizontally">
  <table {...props}><slot /></table>
</div>
